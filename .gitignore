# Package manager and build output
node_modules/
dist/
*.tsbuildinfo

# Reproducible Lean/Lake state; source pins live in formalization/*.toml,
# lake-manifest.json, and lean-toolchain.
formalization/.lake/
formalization/proof-attempt-*.lean

# TypeScript proof runtime state (sessions, runs, SSE artifacts)
.math-agent/

# Research data and local runtime projects
projects/

# Secrets, credentials, local overrides
.env
.env.*
!configs/.env.example
configs/*.local.json
configs/*.local.toml
*.pem
*.key
service-account*.json
/*.json
!/package.json

# Logs & temporary run files
logs/
backups/
*.bak
*.zip

# Legacy Harness metadata, if an old checkout recreates it
deepseek-harness/.git.harness-archive/

# Local presentation source and exports
/ppt/

benchmarks/