# Sum of the first odd numbers

For every natural number $n$ (including $0$ under the empty-sum convention),

\[
\sum_{k=1}^{n}(2k-1)=n^2.
\]

The demo pipeline should retrieve `demo-next-square`, run three distinct workers,
audit the candidate, and archive the resolution.

