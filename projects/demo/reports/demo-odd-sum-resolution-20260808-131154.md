# Sum of the first odd numbers — Resolution

## Scope

Target `demo-odd-sum` only; generated by run `demo-odd-sum-20260808-131154`.

## Allowed dependencies

- `demo-next-square`: Next-square identity

## Frozen branches

- (none)

## Statement

For every natural n, sum_{k=1}^n (2k-1) = n^2.

## Definitions

See the candidate proof below; no definition outside the dependency slice is implicitly authorized.

## Lemmas

Only the PROVED dependencies listed above may be invoked.

## Proof

Summary: Complete induction proof for the demo odd-sum identity.

# Candidate Proof

## Scope

This proof concerns the demo theorem only and uses the allowed dependency `demo-next-square`.

## Statement

For every natural number $n$, the sum of the first $n$ positive odd integers is $n^2$:
\[
\sum_{k=1}^{n}(2k-1)=n^2.
\]

## Definitions

The empty sum for $n=0$ is defined to be $0$.

## Proof

We use induction on $n$. For $n=0$, the left side is the empty sum, hence $0=0^2$.
Assume for some $n\ge 0$ that $\sum_{k=1}^{n}(2k-1)=n^2$. Then
\[
\sum_{k=1}^{n+1}(2k-1)
=\sum_{k=1}^{n}(2k-1)+(2(n+1)-1)
=n^2+2n+1
=(n+1)^2,
\]
where the last equality is the proved dependency `demo-next-square`. This completes the induction.

## Converse / reconstruction

The theorem is an equality, not an iff classification; no converse is claimed.

## Boundary cases

The case $n=0$ was handled explicitly. The first positive case $n=1$ gives $1=1$.

## Computational evidence

None is used.

## Status

CANDIDATE_PROOF pending the outer independent audit gate.

## Converse / reconstruction

Audited according to claim type `equality`.

## Boundary cases

Checked by the Boundary Auditor; see audit results.

## Computational evidence

Any experiments are evidence only. The gate confirms they were kept separate from the proof.

## Audit results

- **boundary_auditor**: PASS — No defect found in the finite induction argument.
- **counterexample_hunter**: PASS — No defect found in the finite induction argument.
- **dependency_auditor**: PASS — No defect found in the finite induction argument.
- **exhaustiveness_auditor**: PASS — No defect found in the finite induction argument.
- **final_proof_auditor**: PASS — The demo induction is complete and uses only the allowed proved identity.

Gate: `PASS`

## Remaining obstruction

None within the stated scope.

## Final classification

PROVED

## Dependencies added

None automatically.

## Status

PROVED — archived only after Final Proof Auditor PASS.
