# Next Attack Queue

Pass B is ready for repair planning, but the following work remains an audit
queue rather than an authorization to modify production behavior.

## P0 — authority and recovery

1. **TruthMutation crash-boundary audit.** Build only isolated deterministic
   fixtures. Exercise intent, prepared record, theorem replace, receipt write,
   restart, duplicate recovery, and concurrent compare boundaries. Prove that
   status-only `PROVED` cannot reconcile without a complete v3 binding marker.
2. **Coverage authority model.** Specify and independently review
   CampaignScopeManifest, anchors, dispositions, transfers, graph cycle rules,
   and coverage-resolution manifest before any root-gate repair is attempted.
3. **Trust and verifier receipts.** Define ResultTrustKernel, TrustReceipt,
   TrustPolicyRef, and mathematical VerifierIndependenceReceipt creation,
   validation, and exact linkage to SessionClosure/AuditGate/Root/Final Audit.
4. **Audit identity binding.** Design the provider→parsed→normalized→persisted
   identity chain and mismatch/omission behavior. Preserve malformed-response
   error semantics without substituting local truth.

## P1 — cross-plane completeness

5. Expand R-01 from the public `RoutedLLMClient.call` seam to every semantic
   public caller, runtime dispatcher, effect coordinator, reconciliation path,
   formalization/certification path, and map-scoped effect path.
6. Audit obligation semantic revision, disposition, control state, decision
   basis, and reverse invalidation together; specifically attack reopen,
   supersede, merge, and evidence invalidation races.
7. Audit root source projection and Final Audit input provenance, including
   source manifest mutation, missing source, stale policy, and consolidation
   rewrite.

## P2 — regression and migration

8. Convert every confirmed gap into deterministic regression cases before
   repair is marked complete; unknown schema/enum/policy compatibility must
   fail closed.
9. Re-run the frozen-main audit after any repair branch is prepared. Keep the
   baseline tag unchanged and require a new audit of any changed proof or
   manifest body.

No queue item authorizes canonical TruthMutation, root synthesis, proof
promotion, or baseline-tag movement during this audit pass.
