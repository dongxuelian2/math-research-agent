# Pre-Root Blocker Repair Matrix

| Finding | Severity | Root cause | Repair | Regression | Adversarial rerun | Status |
|---|---|---|---|---|---|---|
| F-001 stale SessionClosure | P0 | Closure identity and current obligation disposition were not checked before resolution. | Exact map/root/session binding gate; incompatible dispositions fail closed; typed authorized `ScopeTransfer` requires fresh target evidence and revalidation. | `test_f001_stale_closure_isolated_and_explicit_transfer_revalidates`; stale replay twice. | X2 `CERTIFIED`; old closure remains stale and no unintended map version is created. | `CLOSED` |
| F-002 expired-lease authority | P0 | Token and generation were checked, but lease expiry at authoritative ingestion was not. | Expired lease is `STALE_FENCED`; artifact/result provenance is retained, winner/effect preparation is blocked, and reconciliation can review it. | `test_f002_expired_result_is_retained_but_cannot_be_accepted`; X4-style late result. | X4 `CERTIFIED`; `STALE_FENCED`, `authoritative=false`. | `CLOSED` |
| F-003 production EffectSlot bypass | P1 | Normal orchestrator finalization called domain stores without the durable exactly-once boundary. | Internal semantic result + exact binding + `RuntimeEffectCoordinator.apply_effect_once` for SessionClosure, StructuralEffect, governance session/review signals, route failures, and TruthMutation. | Production E2E checks actual slot kinds; crash after domain apply before ACK recovers one domain identity. | X5, X6, X8, X9 `CERTIFIED`; no normal semantic bypass. | `CLOSED` |
| F-004 stranded `DISPATCHED` | P1 | Reconciler handled an orphaned attempt but had no policy for an outbox whose provider acceptance was unknown. | `UNKNOWN_EXECUTION` is durable; `DISPATCHED` moves to `DEAD_LETTER` with `MANUAL_REVIEW_REQUIRED` when no durable payload/result exists. | `AFTER_PROVIDER_RESULT` fault injection; repeated reconciliation is stable. | X12 `CERTIFIED`; attempt `UNKNOWN_EXECUTION`, outbox `DEAD_LETTER`. | `CLOSED` |
| F-005 thesis authorization bypass | P1 | A caller-controlled `revision_reason` could disguise a strategic thesis change as ordinary steering. | Any changed strategic thesis is destructive and requires typed `AUTHORIZED` `PatchAuthorization` matching root/source map identity. | GOV-THESIS-BYPASS rejection plus authorized governance reframe tests. | GOV-THESIS-BYPASS `CERTIFIED`; no unauthorized map mutation. | `CLOSED` |
| F-006 same-model critic independence | P1 | Independence did not enforce the configured `DIFFERENT_MODEL` policy. | Receipt records policy and `same_model`; same-model fallback makes `policy_satisfied=false`, so destructive authorization cannot be authorized. | `test_same_model_critic_fallback_is_not_independent`; authorization negative path. | GOV-SAME-MODEL-FALLBACK `CERTIFIED`. | `CLOSED` |
| F-007 routed cross-plane bindings | P1 | Routed jobs/attempts/results/effects could omit exact ClaimSnapshot and ResearchMap identity. | Immutable `CrossPlaneExecutionBinding` is carried from orchestrator through router/dispatcher and persisted in LogicalJob, Attempt, Result, and EffectSlot; acceptance and preparation validate it. | Binding persistence, stale acceptance, production routed job inspection. | X1 `CERTIFIED`; stale binding cannot enter an effect. | `CLOSED` |

The matrix is repair evidence, not an amendment to the frozen auditor matrix.
`READY_FOR_INDEPENDENT_REAUDIT` is addressed in the regression evidence; Phase 7
authorization remains outside this repairer's authority.
