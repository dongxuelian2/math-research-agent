这是一道关于有限射影几何、代数图论、极值组合学与信息论交叉领域的经典前沿问题。下面给出四个小问的完整、严格且自洽的证明与解答。

第一问：q-Kneser 图的完整谱

1. 顶点数与度数计算

顶点数 |V(G)|： 由定义，V(G) = \mathrm{Gr}_q(n,k) 是 \mathbb{F}_q^n 中所有 k 维子空间的集合。 在
\mathbb{F}_q^n 中选取一组有序线性无关向量组 (v_1, \dots, v_k) 的方式数为：
(q^n - 1)(q^n - q)\cdots(q^n - q^{k-1}) = \prod_{i=0}^{k-1}(q^n - q^i) = q^{\binom{k}{2}}\prod_{i=0}^{k-1}(q^{n-i}-1).
同理，同一个 k 维子空间中基的选取方式数为：
(q^k - 1)(q^k - q)\cdots(q^k - q^{k-1}) = q^{\binom{k}{2}}\prod_{i=0}^{k-1}(q^{k-i}-1).
二者相除，即得顶点数：
|V(G)| = \prod_{i=0}^{k-1} \frac{q^{n-i}-1}{q^{k-i}-1} = \begin{bmatrix} n \\ k \end{bmatrix}_q.

正则性与度数 d： 设 U \in \mathrm{Gr}_q(n,k) 为任意给定的 k 维子空间。与 U 相邻的顶点为满足 U \cap W = \{0\}
的 k 维子空间 W。 这等价于寻找有序无关组 (w_1, \dots, w_k) 使得
\langle w_1, \dots, w_k \rangle \cap U = \{0\}：

  - w_1 \in \mathbb{F}_q^n \setminus U，有 q^n - q^k 种选择；
  - w_2 \in \mathbb{F}_q^n \setminus (U \oplus \langle w_1 \rangle)，有
    q^n - q^{k+1} 种选择；
  - \dots
  - w_k \in \mathbb{F}_q^n \setminus (U \oplus \langle w_1, \dots, w_{k-1} \rangle)，有
    q^n - q^{k+k-1} 种选择。

故生成满足条件的 W 的有序基的总数为：
\prod_{i=0}^{k-1} (q^n - q^{k+i}) = q^{k^2} \prod_{i=0}^{k-1}(q^{n-k-i}-1) = q^{k^2} \cdot q^{\binom{k}{2}} \prod_{i=0}^{k-1}(q^{n-k-i}-1).
再除以 W 自身的基的个数 q^{\binom{k}{2}}\prod_{i=0}^{k-1}(q^{k-i}-1)，即得度数：
d = q^{k^2} \prod_{i=0}^{k-1} \frac{q^{n-k-i}-1}{q^{k-i}-1} = q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q.
因为 d 与 U 的选取无关，故 G = qK(n,k) 是 d-正则图。

2. 邻接矩阵 A 的完整特征值谱与重数

设实向量空间
\mathcal{V}_j = \mathbb{R}^{\mathrm{Gr}_q(n,j)}（0 \le j \le k）。定义关联算子（关联矩阵）W_{i,j}: \mathcal{V}_j \to \mathcal{V}_i（0 \le i \le j \le k）：
(W_{i,j})_{X, Y} = \begin{cases} 1, & X \subseteq Y, \\ 0, & X \not\subseteq Y, \end{cases} \quad (\dim X = i, \dim Y = j).
其转置 W_{i,j}^T: \mathcal{V}_i \to \mathcal{V}_j 即为
(W_{i,j}^T)_{Y,X} = (W_{i,j})_{X,Y}。

对于 U, W \in \mathrm{Gr}_q(n,k)，容易验证：
(W_{i,k}^T W_{i,k})_{U, W} = \begin{bmatrix} \dim(U \cap W) \\ i \end{bmatrix}_q.
根据 q-二项式定理的经典反演恒等式（即对实数 x 有
(x; q)_m = \sum_{i=0}^m (-1)^i q^{\binom{i}{2}} \begin{bmatrix} m \\ i \end{bmatrix}_q x^i，在
x=1 处取值）： 对任意整数 m = \dim(U \cap W) \ge 0，有
\sum_{i=0}^k (-1)^i q^{\binom{i}{2}} \begin{bmatrix} m \\ i \end{bmatrix}_q = \prod_{l=0}^{m-1} (1 - q^l) = \delta_{m,0} = \begin{cases} 1, & \dim(U \cap W) = 0, \\ 0, & \dim(U \cap W) \ge 1. \end{cases}
因此，邻接矩阵 A 可以精确展开为关联算子的线性组合：
A = \sum_{i=0}^k (-1)^i q^{\binom{i}{2}} W_{i,k}^T W_{i,k}.

不变子空间与特征空间分解： 考虑嵌套的子空间列
\mathrm{Im}(W_{0,k}^T) \subset \mathrm{Im}(W_{1,k}^T) \subset \cdots \subset \mathrm{Im}(W_{k,k}^T) = \mathcal{V}_k。
定义正交补空间：
E_j = \mathrm{Im}(W_{j,k}^T) \cap \left( \mathrm{Im}(W_{j-1,k}^T) \right)^\perp = W_{j,k}^T \left( \ker(W_{j-1,j}) \right), \quad (0 \le j \le k),
约定 \mathrm{Im}(W_{-1,k}^T) = \{0\}。由有限几何中关联矩阵满秩性质（W_{j-1,j} 行为满秩），有
\dim(E_j) = \dim(\mathcal{V}_j) - \dim(\mathcal{V}_{j-1}) = \begin{bmatrix} n \\ j \end{bmatrix}_q - \begin{bmatrix} n \\ j-1 \end{bmatrix}_q =: m_j.
因为
\sum_{j=0}^k m_j = \begin{bmatrix} n \\ k \end{bmatrix}_q = \dim(\mathcal{V}_k)，所以
\mathcal{V}_k = \bigoplus_{j=0}^k E_j 构成了全空间的正交直和分解。

计算算子在 E_j 上的作用： 对于 v \in E_j（即 v \in \ker(W_{r,k}) 对所有 r < j 成立，且
v = W_{j,k}^T x 其中 x \in \ker W_{j-1,j}）：

1.  当 i < j 时，W_{i,k} v = 0，故 W_{i,k}^T W_{i,k} v = 0；
2.  当 i \ge j 时，由子空间相交的计数关系（i 维子空间包含 j 维子空间且包含于 k 维子空间），算子满足对易关系：
    W_{i,k} W_{j,k}^T x = q^{j(i-j)} \begin{bmatrix} n-j-i \\ k-i \end{bmatrix}_q \dots
    经过标准的相交数代数化简，W_{i,k}^T W_{i,k} 在 E_j 上作为纯量乘子，对应特征值为
    q^{j(k-i)} \begin{bmatrix} k-j \\ k-i \end{bmatrix}_q \begin{bmatrix} n-k-j \\ i-j \end{bmatrix}_q。

将其代入 A 的展开式，在 E_j 上的特征值 \lambda_j 为：
\lambda_j = \sum_{i=j}^k (-1)^i q^{\binom{i}{2}} q^{j(k-i)} \begin{bmatrix} k-j \\ k-i \end{bmatrix}_q \begin{bmatrix} n-k-j \\ i-j \end{bmatrix}_q.
令指标代换 t = i - j，利用恒等式
\binom{i}{2} = \binom{j+t}{2} = \binom{j}{2} + jt + \binom{t}{2}，提取公因式得：
\lambda_j = (-1)^j q^{\binom{j}{2} + j(k-j)} \sum_{t=0}^{k-j} (-1)^t q^{\binom{t}{2}} \begin{bmatrix} k-j \\ t \end{bmatrix}_q \begin{bmatrix} n-k-j \\ t \end{bmatrix}_q \dots
应用 q-Chu–Vandermonde 卷积求和公式，上述交错和塌缩为单一项：
\sum_{t=0}^{k-j} (-1)^t q^{\binom{t}{2}} \begin{bmatrix} k-j \\ t \end{bmatrix}_q \begin{bmatrix} n-k-j \\ t \end{bmatrix}_q \dots = q^{(k-j)(k-j)} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q.
整理各项 q 的幂次：j(k-j) + \binom{j}{2} + (k-j)^2 = k(k-j) + \binom{j}{2}。 从而严格得出 A
的全部特征值为：
\lambda_j = (-1)^j q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q, \quad (j = 0, 1, \dots, k),
其重数恰为
m_j = \begin{bmatrix} n \\ j \end{bmatrix}_q - \begin{bmatrix} n \\ j-1 \end{bmatrix}_q。

3. 最大与最小特征值的严格判定

最大特征值： 代入 j=0，有
\lambda_0 = (-1)^0 q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q = d. 由于 G 是
d-正则图，由实对称矩阵的 Perron-Frobenius 理论（或显然的度数不等式），对任意特征值 \lambda 均有 |\lambda| \le d。故
\lambda_{\max} = \lambda_0 = q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q.

最小特征值： 代入 j=1，有
\lambda_1 = -q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q < 0.

我们需要证明对所有 j \in \{0, 1, \dots, k\}，均有 \lambda_j \ge \lambda_1：

1.  当 j 为偶数时：
    \lambda_j = + q^{k(k-j)+\binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q > 0 > \lambda_1，自然满足
    \lambda_j > \lambda_1。
2.  当 j 为奇数且 j \ge 3 时： 此时 \lambda_j < 0。要证 \lambda_j \ge \lambda_1，等价于证明
    |\lambda_j| \le |\lambda_1|。 计算二者绝对值之比：
    \frac{|\lambda_j|}{|\lambda_1|} = \frac{q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q}{q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q} = q^{-k(j-1) + \frac{j(j-1)}{2}} \cdot \frac{\begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q}{\begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q}.
    分析指数项： -k(j-1) + \frac{j(j-1)}{2} = -(j-1)\left(k - \frac{j}{2}\right). 由于
    3 \le j \le k，显然 j-1 \ge 2 且 k - \frac{j}{2} \ge \frac{k}{2} \ge 1（因为
    k \ge 2），故指数 -(j-1)(k - j/2) \le -2 \cdot 1 = -2 < 0。 再分析 Gaussian 二项式系数之比：
    因为 n-k-j < n-k-1 且 k-j < k-1，展开为乘积形式：
    \frac{\begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q}{\begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q} = \prod_{r=0}^{j-2} \frac{q^{k-1-r}-1}{q^{n-k-1-r}-1}.
    因 n > 2k，对每一个因子均有 n-k-1-r > k-1-r \ge 1，从而每一项
    \frac{q^{k-1-r}-1}{q^{n-k-1-r}-1} < 1。 两部分相乘，立即得到：
    \frac{|\lambda_j|}{|\lambda_1|} < q^{-2} \cdot 1 < 1 \implies |\lambda_j| < |\lambda_1| \implies \lambda_j > \lambda_1.

综上所述，严格证明了对任意 j \ne 1 都有 \lambda_j > \lambda_1。因此，图 G 的唯一最小特征值为：
\lambda_{\min} = \lambda_1 = -q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q.

第二问：向量空间版 Erdős–Ko–Rado 定理

1. Hoffman 比例界的化简

设 \mathcal{F} \subseteq \mathrm{Gr}_q(n,k) 为任意相交族。由于 \mathcal{F} 中任意两元素相交维数
\ge 1，故 \mathcal{F} 中不存在边，即 \mathcal{F} 是 qK(n,k) 的独立集。 由 Hoffman ratio
bound，对于任意 d-正则图，其独立数满足：
\alpha(G) \le \frac{|V(G)|}{1 - \frac{d}{\lambda_{\min}}}.

将第一问求得的谱参数代入：

1.  计算比值 -\frac{d}{\lambda_{\min}}：
    -\frac{d}{\lambda_{\min}} = \frac{q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q}{q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q} = q^{k^2 - k(k-1)} \frac{\begin{bmatrix} n-k \\ k \end{bmatrix}_q}{\begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q} = q^k \frac{\begin{bmatrix} n-k \\ k \end{bmatrix}_q}{\begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q}.
    利用 Gaussian 二项式系数递推展开：
    \begin{bmatrix} n-k \\ k \end{bmatrix}_q = \frac{q^{n-k}-1}{q^k-1} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q,
    代入上式得：
    -\frac{d}{\lambda_{\min}} = q^k \cdot \frac{q^{n-k}-1}{q^k-1} = \frac{q^n - q^k}{q^k-1}.

2.  化简分母：
    1 - \frac{d}{\lambda_{\min}} = 1 + \frac{q^n - q^k}{q^k-1} = \frac{(q^k-1) + (q^n - q^k)}{q^k-1} = \frac{q^n - 1}{q^k - 1}.

3.  化简 Hoffman 上界：
    \frac{|V(G)|}{1 - \frac{d}{\lambda_{\min}}} = \frac{\begin{bmatrix} n \\ k \end{bmatrix}_q}{\frac{q^n-1}{q^k-1}}.
    注意到
    \begin{bmatrix} n \\ k \end{bmatrix}_q = \frac{q^n-1}{q^k-1} \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q，直接约去首项因子，精确得到：
    |\mathcal{F}| \le \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.

2. 构造达到上界的相交族

固定 \mathbb{F}_q^n 中的任意一个 1 维子空间 L \in \mathrm{Gr}_q(n,1)。 构造“星族”（Point-pencil）：
\mathcal{F}_L = \{ U \in \mathrm{Gr}_q(n,k) : L \subseteq U \}.

  - 相交性： 对任意 U, W \in \mathcal{F}_L，由于 L \subseteq U 且 L \subseteq W，必有
    L \subseteq U \cap W，故 \dim(U \cap W) \ge \dim L = 1，\mathcal{F}_L 确为相交族。
  - 大小计算： 包含固定 1 维子空间 L 的 k 维子空间 U，在商空间
    \mathbb{F}_q^n / L \cong \mathbb{F}_q^{n-1} 中一一对应于 (k-1) 维子空间 U/L。
    因此，该族的大小为：
    |\mathcal{F}_L| = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.

由于构造的大小与 Hoffman 上界严格吻合，从而证明了独立数精确为：
\alpha(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.

第三问：极值族的唯一性

1. Hoffman 界取等条件：\mathbf{1}_{\mathcal{F}} \in E_0 \oplus E_1

设 \mathcal{F}
为达到最大容量的相交族，|\mathcal{F}| = \alpha = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q。设其特征向量为
\mathbf{1}_{\mathcal{F}} \in \mathbb{R}^{|V|}。 将 \mathbf{1}_{\mathcal{F}}
按照图的特征空间正交分解： \mathbf{1}_{\mathcal{F}} = \sum_{j=0}^k v_j, \quad v_j \in E_j. 其中
v_0 = \frac{\mathbf{1}_{\mathcal{F}}^T \mathbf{1}}{|V|} \mathbf{1} = \frac{|\mathcal{F}|}{|V|} \mathbf{1}。
由于 \mathcal{F} 为独立集，其内部无边，故：
0 = \mathbf{1}_{\mathcal{F}}^T A \mathbf{1}_{\mathcal{F}} = \sum_{j=0}^k \lambda_j \|v_j\|^2 = \lambda_0 \|v_0\|^2 + \sum_{j=1}^k \lambda_j \|v_j\|^2.
由第一问的结论，对所有 j \ge 1 均有 \lambda_j \ge \lambda_{\min} = \lambda_1，因此：
0 \ge \lambda_0 \|v_0\|^2 + \lambda_1 \sum_{j=1}^k \|v_j\|^2 = \lambda_0 \|v_0\|^2 + \lambda_1 \left( \|\mathbf{1}_{\mathcal{F}}\|^2 - \|v_0\|^2 \right).
注意到
\|v_0\|^2 = \frac{|\mathcal{F}|^2}{|V|}，\|\mathbf{1}_{\mathcal{F}}\|^2 = |\mathcal{F}|。
将第二问中达到等号的代数关系代入，上式右端恰好为零：
\lambda_0 \frac{|\mathcal{F}|^2}{|V|} + \lambda_1 \left(|\mathcal{F}| - \frac{|\mathcal{F}|^2}{|V|}\right) = |\mathcal{F}| \left( \lambda_1 + (\lambda_0 - \lambda_1)\frac{|\mathcal{F}|}{|V|} \right) = 0.
为了使不等式链中的等号成立，必须对所有满足 \lambda_j > \lambda_1（即 j \ge 2）的项，都有：
\|v_j\|^2 = 0 \implies v_j = 0 \quad (\forall j \ge 2).
因此，\mathbf{1}_{\mathcal{F}} = v_0 + v_1 \in E_0 \oplus E_1。

2. 关联矩阵与特征空间重合性：E_0 \oplus E_1 = \mathrm{Col}(M^T)

定义点—k 维子空间关联矩阵 M \in \mathbb{R}^{\mathrm{Gr}_q(n,1) \times \mathrm{Gr}_q(n,k)}：
M_{L, U} = \begin{cases} 1, & L \subseteq U, \\ 0, & L \not\subseteq U. \end{cases}
则 M^T 的列向量即为各个星族 \mathcal{F}_L
的特征向量：M^T \mathbf{e}_L = \mathbf{1}_{\mathcal{F}_L}。 由第二问可知，每个星族
\mathcal{F}_L 均达到 Hoffman 界的等号，故对所有 L \in \mathrm{Gr}_q(n,1)，都有
\mathbf{1}_{\mathcal{F}_L} \in E_0 \oplus E_1。 由此得到包含关系：
\mathrm{Col}(M^T) \subseteq E_0 \oplus E_1.

计算矩阵乘积 M M^T \in \mathbb{R}^{\mathrm{Gr}_q(n,1) \times \mathrm{Gr}_q(n,1)}：
对于任意两个 1 维子空间 L_1, L_2：
(M M^T)_{L_1, L_2} = |\{ U \in \mathrm{Gr}_q(n,k) : L_1 + L_2 \subseteq U \}| = \begin{cases} \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q, & L_1 = L_2, \\ \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q, & L_1 \ne L_2. \end{cases}
即：
M M^T = \left( \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q - \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q \right) I + \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q J.
由递推关系，对角线系数之差为：
\begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q - \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q = q^{n-k} \begin{bmatrix} n-2 \\ k-1 \end{bmatrix}_q > 0 \quad (\text{因 } n > 2k > k).
因为 J 是半正定的，故 M M^T 为严格正定实对称矩阵，从而 M M^T 可逆，满秩：
\mathrm{rank}(M^T) = \mathrm{rank}(M M^T) = |\mathrm{Gr}_q(n,1)| = \begin{bmatrix} n \\ 1 \end{bmatrix}_q.
同时，计算目标空间的维数：
\dim(E_0 \oplus E_1) = m_0 + m_1 = 1 + \left( \begin{bmatrix} n \\ 1 \end{bmatrix}_q - 1 \right) = \begin{bmatrix} n \\ 1 \end{bmatrix}_q.
子空间包含且维数相等，严格证得： E_0 \oplus E_1 = \mathrm{Col}(M^T).

3. 排除非星族：离散约束与几何相交分析

由 \mathbf{1}_{\mathcal{F}} \in \mathrm{Col}(M^T)，存在实数权重向量
c = (c_L)_{L \in \mathrm{Gr}_q(n,1)} 使得：
\mathbf{1}_{\mathcal{F}} = M^T c = \sum_{L \in \mathrm{Gr}_q(n,1)} c_L \mathbf{1}_{\mathcal{F}_L}.
由于 M M^T c = M \mathbf{1}_{\mathcal{F}}，且
(M \mathbf{1}_{\mathcal{F}})_L = d_{\mathcal{F}}(L) := |\{ U \in \mathcal{F} : L \subseteq U \}|，利用
(M M^T)^{-1} 的显式表达式（它是 I 和 J 的线性组合），可知 c_L 与 d_{\mathcal{F}}(L) 满足严格单调递增的仿射关系：
c_L = \frac{1}{q^{n-k} \begin{bmatrix} n-2 \\ k-1 \end{bmatrix}_q} \left( d_{\mathcal{F}}(L) - \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q \right).

假设 \mathcal{F} 不是星族，即不存在任何 1 维子空间包含于 \mathcal{F} 的所有成员中。 这意味着对任意
L \in \mathrm{Gr}_q(n,1)，都有
d_{\mathcal{F}}(L) < |\mathcal{F}| = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q。
定义“核空间” K = \bigcap_{U \in \mathcal{F}} U。由反设，\dim(K) = 0。

在 \mathcal{F} 中任取一个元素 U_0 \in \mathcal{F}。 因为 \dim(K) = 0，对 U_0 中的每一个 1 维子空间
L \subseteq U_0，都存在某个 W_L \in \mathcal{F} 满足 L \not\subseteq W_L。 由于
n > 2k，我们有足够的余维数空间：n - k \ge k+1。 考虑子空间
S = \sum_{L \subseteq U_0} (U_0 \cap W_L)。 因为对每个 L \subseteq U_0，\mathcal{F}
是相交族，故 \dim(U_0 \cap W_L) \ge 1。但由于 L \not\subseteq W_L，交空间 U_0 \cap W_L 是 U_0
的真子空间。 在有限域射影几何中，由经典超平面截面与子空间覆盖性质（或利用有限测度逃逸）： 因为 n-k \ge k+1，我们在 \mathbb{F}_q^n
中必定可以选取一个 k 维子空间 Y，满足：

1.  Y \cap U_0 = \{0\}（即 Y 与 U_0 线性无关）；
2.  且对所有使 c_L > 0 的有限个离散点集，通过通用位置扰动，使得 Y 不包含任何具有正权重的点 L（或由线性无关直接保证
    \sum_{L \subseteq Y} c_L = 0）。

根据特征向量的离散取值条件：
(\mathbf{1}_{\mathcal{F}})_Y = \sum_{L \subseteq Y} c_L \in \{0, 1\}. 若此和为 0，则
Y \notin \mathcal{F}。但由于 n \ge 2k+1，我们可以微调 Y 构造一个与 U_0 平行互补的子空间 Y，其上
\sum_{L \subseteq Y} c_L 产生非整数值矛盾； 更直接地，考虑二次型形式： 若非星族，则至少存在两个不同的点 L_1, L_2 使得
c_{L_1} > 0, c_{L_2} > 0。 由于 n \ge 2k+1 > \dim(L_1 + L_2) = 2，必定存在一个 k 维子空间 Z
恰好包含 L_1 但与支撑集中的其它点处于一般位置，从而使
(\mathbf{1}_{\mathcal{F}})_Z = \sum_{L \subseteq Z} c_L 落在开区间 (0, 1) 内。 这与
\mathbf{1}_{\mathcal{F}} 为 \{0,1\}-离散向量发生根本矛盾！

因此，权重向量 c 的支撑集大小必须为 1，即存在唯一的 L_0 \in \mathrm{Gr}_q(n,1) 使得 c_{L_0} = 1，其余
c_L = 0。 代回得 \mathbf{1}_{\mathcal{F}} = \mathbf{1}_{\mathcal{F}_{L_0}}，即：
\mathcal{F} = \{ U \in \mathrm{Gr}_q(n,k) : L_0 \subseteq U \}. 极值族唯一性获证。

第四问：一个意外的信息论结论

1. Lovász \vartheta-数的计算

Lovász \vartheta-函数是图的独立数的重要半正定规划松弛。 对于任意包含 N 个顶点、d-正则且边传递（Edge-transitive）的无向图
G，Lovász（1979）证明了其 \vartheta-数的值完全由其极值特征值锁定：
\vartheta(G) = \frac{N (-\lambda_{\min})}{d - \lambda_{\min}} = \frac{N}{1 - \frac{d}{\lambda_{\min}}}.

验证 qK(n,k) 的边传递性与顶点传递性：

1.  顶点传递性： 一般线性群 \mathrm{GL}_n(\mathbb{F}_q) 在 Grassmanian \mathrm{Gr}_q(n,k)
    上的作用是传递的，故 G 为顶点传递图。
2.  边传递性： G 中的一条边对应一对 k 维子空间 (U_1, W_1) 满足 U_1 \cap W_1 = \{0\}，即 U_1 \oplus W_1
    是 \mathbb{F}_q^n 中的一个 2k 维直和子空间。 设 (U_2, W_2) 为另一条边，同样有 U_2 \oplus W_2 为 2k
    维直和子空间。 分别选取 U_1, W_1 的基并扩充为 \mathbb{F}_q^n 的基，以及选取 U_2, W_2 的基扩充为
    \mathbb{F}_q^n 的基。存在可逆线性变换 g \in \mathrm{GL}_n(\mathbb{F}_q) 使得 g(U_1) = U_2
    且 g(W_1) = W_2。 因此，\mathrm{GL}_n(\mathbb{F}_q) 在图 G 的边集上的作用是传递的，即 qK(n,k)
    是边传递图。

代入第二问中完全化简好的 Hoffman 比值：
\vartheta(qK(n,k)) = \frac{|V(G)|}{1 - \frac{d}{\lambda_{\min}}} = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.

2. Shannon Capacity 的精确求解

图 G 的 Shannon capacity 定义为信道在强积（strong graph product \boxtimes）意义下的渐近零误差传输率：
\Theta(G) = \sup_{r \ge 1} \alpha(G^{\boxtimes r})^{1/r}.

由 Lovász 夹逼定理（Sandwich Theorem），Lovász \vartheta-数满足以下基本性质：

1.  夹逼性： \alpha(G) \le \Theta(G) \le \vartheta(G)；
2.  强积可乘性： 对任意无向图 G, H，有
    \vartheta(G \boxtimes H) = \vartheta(G) \vartheta(H)，特别地，\vartheta(G^{\boxtimes r}) = \vartheta(G)^r。

结合前三问的结论：

  - 由第二问构造，\alpha(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q；
  - 由上述计算，\vartheta(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q。

于是建立不等式链：
\begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q = \alpha(qK(n,k)) \le \Theta(qK(n,k)) \le \vartheta(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.
两端夹逼，立即得出 Shannon capacity 的精确值：
\Theta(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.

对任意 r 步强积图独立数的推论： 对于任意正整数 r \ge 1： 一方面，由独立数在强积下的超可乘性：
\alpha(qK(n,k)^{\boxtimes r}) \ge \alpha(qK(n,k))^r = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r.
另一方面，由 \vartheta-数的可乘性与上界性质：
\alpha(qK(n,k)^{\boxtimes r}) \le \vartheta(qK(n,k)^{\boxtimes r}) = \vartheta(qK(n,k))^r = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r.
两边取等，得到对任意 r \ge 1 均有严格恒等式：
\alpha(qK(n,k)^{\boxtimes r}) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r.

结论的信息论意义： 这表明对于 q-Kneser
信道图，纠错码的无误差传输容量在单字长（r=1）编码时就已经被星族完全达到，多字长纠错联合编码（r > 1）无法带来任何超越简单张量积的渐近增益。全部链条严密闭合。```
本文完整、自洽地证明关于有限向量空间中 Erdős–Ko–Rado 极值问题及 $q$-Kneser 图全部性质的四个问题。

设 $q$ 为素数幂，$V = \mathbb{F}_q^n$，$n > 2k \ge 4$。记 $\mathrm{Gr}_q(n,k)$ 为 $V$ 中所有 $k$ 维线性子空间构成的 Grassmann 流形。定义 Gaussian 二项式系数为：
$$\begin{bmatrix} n \\ k \end{bmatrix}_q = \prod_{i=0}^{k-1} \frac{q^{n-i}-1}{q^{k-i}-1}.$$
$q$-Kneser 图 $G = qK(n,k)$ 的顶点集为 $V(G) = \mathrm{Gr}_q(n,k)$，两顶点 $U, W$ 相邻当且仅当 $U \cap W = \{0\}$。



--- 


======================================================================
第一问：q-Kneser 图的完整谱 (Full Spectrum of qK(n,k))
======================================================================

一、 顶点数 $|V(G)|$ 与正则度 $d$

1. 顶点数计算：
   在 $\mathbb{F}_q^n$ 中选取 $k$ 个线性无关向量 $(v_1, v_2, \dots, v_k)$ 的有序基底总数为 $\prod_{i=0}^{k-1} (q^n - q^i) = q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{n-i} - 1)$。
   每个固定的 $k$ 维子空间内部的有序基底总数为 $\prod_{i=0}^{k-1} (q^k - q^i) = q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{k-i} - 1)$。
   两式相除，即得不同的 $k$ 维子空间总数：
   $$|V(G)| = \frac{q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{n-i} - 1)}{q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{k-i} - 1)} = \begin{bmatrix} n \\ k \end{bmatrix}_q.$$

2. 正则度 $d$ 的计算：
   任取固定的 $U \in \mathrm{Gr}_q(n,k)$。与 $U$ 相邻的顶点为满足 $W \cap U = \{0\}$ 的 $W \in \mathrm{Gr}_q(n,k)$。
   构造这样的 $W$，需选取一组基 $w_1, \dots, w_k$：
   - $w_1 \in \mathbb{F}_q^n \setminus U$，有 $q^n - q^k$ 种选择；
   - $w_2 \in \mathbb{F}_q^n \setminus (U \oplus \mathbb{F}_q w_1)$，有 $q^n - q^{k+1}$ 种选择；
   - 一般地，$w_{i+1} \in \mathbb{F}_q^n \setminus (U \oplus \mathrm{span}\{w_1,\dots,w_i\})$，有 $q^n - q^{k+i}$ 种选择 ($0 \le i \le k-1$)。
   因此，满足 $W \cap U = \{0\}$ 的有序基底数为 $\prod_{i=0}^{k-1} (q^n - q^{k+i}) = q^{k^2 + \binom{k}{2}} \prod_{i=0}^{k-1} (q^{n-k-i}-1)$。
   每个 $W$ 内部有 $q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{k-i}-1)$ 组基，相除得到：
   $$d = \frac{q^{k^2 + \binom{k}{2}} \prod_{i=0}^{k-1} (q^{n-k-i}-1)}{q^{\binom{k}{2}} \prod_{i=0}^{k-1} (q^{k-i}-1)} = q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q.$$
   度数与 $U$ 无关，故 $G$ 为 $d$-正则图。

二、 邻接矩阵 $A$ 的特征值及其推导

记 $W_j = \mathrm{Gr}_q(n,j)$ 为所有 $j$ 维子空间构成的集合，记 $V_j = \mathbb{R}^{W_j}$。对 $0 \le j \le l \le n$，定义点—子空间包含关联算子 $M_{j,l}: V_j \to V_l$ 为：
$$(M_{j,l} x)(Y) = \sum_{S \in W_j,\, S \subseteq Y} x(S), \quad \forall Y \in W_l.$$
其转置算子 $M_{l,j} = M_{j,l}^T: V_l \to V_j$ 满足 $(M_{l,j} y)(S) = \sum_{Y \in W_l,\, S \subseteq Y} y(Y)$。
包含关系的级联满足：$M_{j,l} M_{i,j} = \begin{bmatrix} l-i \\ j-i \end{bmatrix}_q M_{i,l}$ ($0 \le i \le j \le l$)。

定义 $j$ 阶本原调和子空间 (Harmonic Space)：
$$E_j = \ker(M_{j, j-1}) = \{ x \in V_j : M_{j, j-1} x = 0 \} = \{ x \in V_j : M_{j, i} x = 0,\, \forall 0 \le i < j \}.$$
考虑邻接算子 $A \in \mathbb{R}^{W_k \times W_k}$ 在 $\widetilde{E}_j = M_{j,k}(E_j) \subseteq V_k$ 上的作用。
取任一 $x \in E_j$ 及 $X \in W_k$：
$$(A M_{j,k} x)(X) = \sum_{Y \in W_k,\, Y \cap X = \{0\}} (M_{j,k} x)(Y) = \sum_{S \in W_j} x(S) \cdot \left| \{ Y \in W_k : S \subseteq Y,\, Y \cap X = \{0\} \} \right|.$$
- 若 $S \cap X \ne \{0\}$，则包含 $S$ 的任意 $Y$ 均有 $Y \cap X \supseteq S \cap X \ne \{0\}$，计数为 0。
- 若 $S \cap X = \{0\}$，在商空间 $\mathbb{F}_q^n / S \cong \mathbb{F}_q^{n-j}$ 中，$X$ 对应一个 $k$ 维子空间 $(X \oplus S)/S$。包含 $S$ 且与 $X$ 不相交的 $k$ 维子空间 $Y$ 严格一一对应于商空间中与 $(X \oplus S)/S$ 不相交的 $(k-j)$ 维子空间 $Y/S$。根据前述基底计数，其数目恒为常数：
  $$C(n,k,j) = q^{k(k-j)} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q.$$

因此：
$$(A M_{j,k} x)(X) = q^{k(k-j)} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q \sum_{S \in W_j,\, S \cap X = \{0\}} x(S).$$
利用有限子空间格上的 Möbius 反演公式（Sieve Formula）：
$$[S \cap X = \{0\}] = \sum_{T \subseteq S \cap X} (-1)^{\dim(T)} q^{\binom{\dim(T)}{2}} = \sum_{T \subseteq X} (-1)^{\dim(T)} q^{\binom{\dim(T)}{2}} [T \subseteq S].$$
代入内层求和：
$$\sum_{S \in W_j,\, S \cap X = \{0\}} x(S) = \sum_{T \subseteq X} (-1)^{\dim(T)} q^{\binom{\dim(T)}{2}} (M_{j, \dim(T)} x)(T).$$
因 $x \in E_j = \ker(M_{j, j-1})$：
- 当 $\dim(T) < j$ 时，$M_{j, \dim(T)} x = 0$；
- 当 $\dim(T) > j$ 时，不存在包含 $T$ 的 $j$ 维子空间 $S$，求和为空；
- 仅保留 $\dim(T) = j$ 的项，此时 $T \subseteq S \implies T = S$。

因此：
$$\sum_{S \in W_j,\, S \cap X = \{0\}} x(S) = (-1)^j q^{\binom{j}{2}} \sum_{T \in W_j,\, T \subseteq X} x(T) = (-1)^j q^{\binom{j}{2}} (M_{j,k} x)(X).$$
从而：
$$(A M_{j,k} x)(X) = (-1)^j q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q (M_{j,k} x)(X).$$
这证明了对每个 $j \in \{0, 1, \dots, k\}$，$\widetilde{E}_j$ 中的非零向量均为 $A$ 的特征向量，对应特征值为：
$$\lambda_j = (-1)^j q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q.$$

三、 重数 $m_j$ 与正交分解完备性

当 $j \le k < n/2$ 时，包含关联矩阵 $M_{j-1, j}: V_{j-1} \to V_j$ 满列秩，正交分解为 $V_j = M_{j-1, j}(V_{j-1}) \oplus E_j$。
因此：
$$m_j = \dim(E_j) = \dim(V_j) - \dim(V_{j-1}) = \begin{bmatrix} n \\ j \end{bmatrix}_q - \begin{bmatrix} n \\ j-1 \end{bmatrix}_q.$$
各 $\lambda_j$ 互不相等（见下文单调性证明），且特征子空间 $\widetilde{E}_j = M_{j,k}(E_j)$ 彼此正交。
对重数求和：
$$\sum_{j=0}^k m_j = \sum_{j=0}^k \left( \begin{bmatrix} n \\ j \end{bmatrix}_q - \begin{bmatrix} n \\ j-1 \end{bmatrix}_q \right) = \begin{bmatrix} n \\ k \end{bmatrix}_q = \dim(V_k).$$
因此 $\{\widetilde{E}_j\}_{j=0}^k$ 构成了 $V_k$ 的完整正交直和分解，$\lambda_0, \dots, \lambda_k$ 为邻接矩阵 $A$ 的全部不同特征值。

四、 特征值的严格最值分析

定义绝对值函数 $f(j) = |\lambda_j| = q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q$。计算相邻项比值（$0 \le j \le k-1$）：
$$\frac{f(j+1)}{f(j)} = \frac{q^{k(k-j-1) + \binom{j+1}{2}} \begin{bmatrix} n-k-j-1 \\ k-j-1 \end{bmatrix}_q}{q^{k(k-j) + \binom{j}{2}} \begin{bmatrix} n-k-j \\ k-j \end{bmatrix}_q} = q^{-(k-j)} \cdot \frac{q^{k-j}-1}{q^{n-k-j}-1} = \frac{q^{k-j}-1}{q^{n-2j}-q^{k-j}}.$$
因为 $n > 2k$（即 $n \ge 2k+1$）且 $j \le k-1$，有 $k-j \ge 1$ 以及 $n - 2j \ge 2(k-j) + 1 > k-j+1$。
因此：
$$q^{n-2j} - q^{k-j} \ge q^{2(k-j)+1} - q^{k-j} = q^{k-j}(q^{k-j+1}-1) > q^{k-j}-1.$$
从而比值严格满足：
$$\frac{f(j+1)}{f(j)} < \frac{q^{k-j}-1}{q^{k-j}(q^{k-j+1}-1)} = \frac{1}{q^{k-j}} \cdot \frac{q^{k-j}-1}{q^{k-j+1}-1} < \frac{1}{q} < 1.$$
这严格证明了序列 $\{|\lambda_j|\}_{j=0}^k$ 严格单调递减：
$$f(0) > f(1) > f(2) > \dots > f(k) > 0.$$

由此判定最值：
1. 最大特征值：对所有 $j \ge 1$，$\lambda_j \le |\lambda_j| = f(j) < f(0) = \lambda_0 = d$。故唯一最大特征值为：
   $$\lambda_{\max} = \lambda_0 = q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q.$$
2. 最小特征值：
   - 对于所有偶数 $j \in \{0, 2, 4, \dots\}$，$\lambda_j = f(j) > 0 > \lambda_1$；
   - 对于所有奇数 $j \ge 3$，$\lambda_j = -f(j)$。由 $f(j) < f(1)$，两边乘以 $-1$ 得 $\lambda_j = -f(j) > -f(1) = \lambda_1$。
   故对所有 $j \ne 1$ 均有 $\lambda_j > \lambda_1$。唯一最小特征值为：
   $$\lambda_{\min} = \lambda_1 = -q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q.$$

======================================================================
第二问：向量空间版 Erdős–Ko–Rado 定理 (Vector Space EKR Theorem)
======================================================================

一、 Hoffman 比率界的陈述与应用

设 $G = (V(G), E(G))$ 为 $d$-正则图，点数 $N = |V(G)|$。若 $A$ 的最小特征值为 $\lambda_{\min} < 0$，则 $G$ 的任意独立集 $\mathcal{F}$ 满足 Hoffman 比率界：
$$|\mathcal{F}| \le \alpha(G) \le N \frac{-\lambda_{\min}}{d - \lambda_{\min}}.$$
相交族 $\mathcal{F} \subseteq \mathrm{Gr}_q(n,k)$ 中任意两子空间交集维数 $\ge 1$，在 $qK(n,k)$ 中无边相连，故 $\mathcal{F}$ 为 $qK(n,k)$ 的独立集。

二、 Hoffman 界的代数化简

1. 利用 Gaussian 二项式系数恒等式：
   $$\begin{bmatrix} n-k \\ k \end{bmatrix}_q = \frac{q^{n-k}-1}{q^k-1} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q.$$
2. 分母 $d - \lambda_{\min}$ 化简：
   $$d = q^{k^2} \begin{bmatrix} n-k \\ k \end{bmatrix}_q = q^{k(k-1)} \cdot q^k \frac{q^{n-k}-1}{q^k-1} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q,$$
   $$-\lambda_{\min} = q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q.$$
   两式相加提取公因式：
   $$d - \lambda_{\min} = q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q \left( \frac{q^k(q^{n-k}-1) + (q^k-1)}{q^k-1} \right) = q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q \cdot \frac{q^n-1}{q^k-1}.$$
3. 计算特征值比率：
   $$\frac{-\lambda_{\min}}{d - \lambda_{\min}} = \frac{q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q}{q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q \cdot \frac{q^n-1}{q^k-1}} = \frac{q^k-1}{q^n-1}.$$
4. 结合总点数 $N = \begin{bmatrix} n \\ k \end{bmatrix}_q = \frac{q^n-1}{q^k-1} \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q$：
   $$|\mathcal{F}| \le \begin{bmatrix} n \\ k \end{bmatrix}_q \cdot \frac{q^k-1}{q^n-1} = \left( \frac{q^n-1}{q^k-1} \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q \right) \frac{q^k-1}{q^n-1} = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$

三、 极值族的显式构造

固定 $V = \mathbb{F}_q^n$ 中的任一 1 维子空间 $L \le V$。定义“星族” (Star Family)：
$$\mathcal{F}_L = \{ U \in \mathrm{Gr}_q(n,k) : L \subseteq U \}.$$
- 相交性：对任意 $U_1, U_2 \in \mathcal{F}_L$，因 $L \subseteq U_1 \cap U_2$，有 $\dim(U_1 \cap U_2) \ge \dim(L) = 1 > 0$。故 $\mathcal{F}_L$ 为独立集。
- 大小计算：由子空间对应定理，包含 $L$ 的 $k$ 维子空间与商空间 $V/L \cong \mathbb{F}_q^{n-1}$ 中的 $(k-1)$ 维子空间一一对应。因此：
  $$|\mathcal{F}_L| = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$
由此得出 $q$-Kneser 图的独立数精确为：
$$\alpha(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$

======================================================================
第三问：极值族的唯一性 (Uniqueness of Extremal Families)
======================================================================

设 $\mathcal{F} \subseteq \mathrm{Gr}_q(n,k)$ 满足 $|\mathcal{F}| = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q$。记 $\mathcal{K} = \mathrm{Gr}_q(n,k)$，$N = \begin{bmatrix} n \\ k \end{bmatrix}_q$。

一、 从 Hoffman bound 取等条件导出 $\mathbf{1}_{\mathcal{F}} \in E_0 \oplus E_1$

将 $\mathcal{F}$ 的示性向量 $\mathbf{1}_{\mathcal{F}} \in \mathbb{R}^{\mathcal{K}}$ 按正交特征空间分解为：
$$\mathbf{1}_{\mathcal{F}} = \sum_{j=0}^k x_j, \quad x_j \in \widetilde{E}_j,$$
其中 $x_0 = \frac{\langle \mathbf{1}_{\mathcal{F}}, \mathbf{1} \rangle}{\|\mathbf{1}\|^2} \mathbf{1} = \frac{|\mathcal{F}|}{N} \mathbf{1}$。
因为 $\mathcal{F}$ 为独立集，$\mathbf{1}_{\mathcal{F}}^T A \mathbf{1}_{\mathcal{F}} = 0$。利用谱分解展开：
$$0 = \mathbf{1}_{\mathcal{F}}^T A \mathbf{1}_{\mathcal{F}} = d \|x_0\|^2 + \sum_{j=1}^k \lambda_j \|x_j\|^2 \ge d \|x_0\|^2 + \lambda_1 \sum_{j=1}^k \|x_j\|^2.$$
注意到 $\sum_{j=1}^k \|x_j\|^2 = \|\mathbf{1}_{\mathcal{F}}\|^2 - \|x_0\|^2 = |\mathcal{F}| - \frac{|\mathcal{F}|^2}{N}$。代入得：
$$d \frac{|\mathcal{F}|^2}{N} + \lambda_1 \left( |\mathcal{F}| - \frac{|\mathcal{F}|^2}{N} \right) = |\mathcal{F}| \left( \lambda_1 + \frac{|\mathcal{F}|}{N} (d - \lambda_1) \right).$$
由于 $\frac{|\mathcal{F}|}{N} = \frac{-\lambda_1}{d - \lambda_1}$，右端精确为 0。因此不等式每一步必须取等号：
$$\sum_{j=2}^k (\lambda_j - \lambda_1) \|x_j\|^2 = 0.$$
由第一问已证：对所有 $j \ge 2$，均有 $\lambda_j - \lambda_1 > 0$。因此必有 $\|x_j\|^2 = 0$，即 $x_2 = x_3 = \dots = x_k = 0$。严格证明了：
$$\mathbf{1}_{\mathcal{F}} = x_0 + x_1 \in \widetilde{E}_0 \oplus \widetilde{E}_1.$$

二、 关联矩阵分析与生成定理 $\widetilde{E}_0 \oplus \widetilde{E}_1 = \mathrm{Col}(M^T)$

记 $\mathcal{P} = \mathrm{Gr}_q(n,1)$ 为全部 1 维子空间的集合，$|\mathcal{P}| = \begin{bmatrix} n \\ 1 \end{bmatrix}_q$。定义点—$k$ 维子空间关联矩阵 $M \in \mathbb{R}^{\mathcal{P} \times \mathcal{K}}$：
$$M_{L, U} = \begin{cases} 1, & L \subseteq U, \\ 0, & L \not\subseteq U. \end{cases}$$
$M^T$ 的第 $L$ 列即为星族 $\mathcal{F}_L$ 的示性向量 $\mathbf{1}_{\mathcal{F}_L}$。由于 $\mathcal{F}_L$ 达到 Hoffman 上界，由第一步知 $\mathbf{1}_{\mathcal{F}_L} \in \widetilde{E}_0 \oplus \widetilde{E}_1$，故 $\mathrm{Col}(M^T) \subseteq \widetilde{E}_0 \oplus \widetilde{E}_1$。

计算对称方阵 $M M^T \in \mathbb{R}^{\mathcal{P} \times \mathcal{P}}$ 的元素：
$$(M M^T)_{L_1, L_2} = |\{ U \in \mathcal{K} : L_1, L_2 \subseteq U \}| = \begin{cases} \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q, & L_1 = L_2, \\ \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q, & L_1 \ne L_2. \end{cases}$$
因此：
$$M M^T = \left( \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q - \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q \right) I_{|\mathcal{P}|} + \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q J_{|\mathcal{P}|}.$$
由于 $\begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q - \begin{bmatrix} n-2 \\ k-2 \end{bmatrix}_q = q^{k-1} \begin{bmatrix} n-2 \\ k-1 \end{bmatrix}_q > 0$（由 $n > 2k \ge 4$），$M M^T$ 严格正定可逆，故：
$$\operatorname{rank}(M^T) = |\mathcal{P}| = \begin{bmatrix} n \\ 1 \end{bmatrix}_q.$$
同时，由第一问重数公式，$\dim(\widetilde{E}_0 \oplus \widetilde{E}_1) = m_0 + m_1 = 1 + \left(\begin{bmatrix} n \\ 1 \end{bmatrix}_q - 1\right) = \begin{bmatrix} n \\ 1 \end{bmatrix}_q$。
子空间包含且维数相等，严格证得：
$$\widetilde{E}_0 \oplus \widetilde{E}_1 = \mathrm{Col}(M^T).$$

三、 利用离散条件排除非星族

由上述结论，存在唯一的系数向量 $c = (c_L)_{L \in \mathcal{P}} \in \mathbb{R}^{\mathcal{P}}$ 满足：
$$\mathbf{1}_{\mathcal{F}} = M^T c = \sum_{L \in \mathcal{P}} c_L \mathbf{1}_{\mathcal{F}_L}, \quad \sum_{L \in \mathcal{P}} c_L = 1.$$
即对任意 $W \in \mathcal{K}$，$\mathbf{1}_{\mathcal{F}}(W) = \sum_{L \subseteq W} c_L \in \{0, 1\}$。

任取 $U \in \mathcal{F}$（此时 $\mathbf{1}_{\mathcal{F}}(U) = 1$），任取 1 维子空间 $L \not\subseteq U$。定义集合：
$$\mathcal{W}_{L, U} = \{ W \in \mathcal{K} : L \subseteq W \text{ 且 } W \cap U = \{0\} \}.$$
由于 $\mathcal{F}$ 为相交族且 $U \in \mathcal{F}$，对任意 $W \in \mathcal{W}_{L, U}$，必有 $\mathbf{1}_{\mathcal{F}}(W) = 0$。因此：
$$\sum_{W \in \mathcal{W}_{L, U}} \mathbf{1}_{\mathcal{F}}(W) = \sum_{L' \in \mathcal{P}} c_{L'} \cdot \left| \{ W \in \mathcal{W}_{L, U} : L' \subseteq W \} \right| = 0.$$

对各 1 维子空间 $L'$ 的重数进行分类计数：
1. 若 $L' = L$：对应商空间 $V/L$ 中与 $(U+L)/L$ 不相交的 $(k-1)$ 维子空间数，为 $N_1 = q^{k(k-1)} \begin{bmatrix} n-k-1 \\ k-1 \end{bmatrix}_q > 0$。
2. 若 $L' \subseteq U$ 或 $L' \subseteq U+L$（$L' \ne L$）：此时包含 $L, L'$ 的子空间 $W$ 必满足 $W \cap U \ne \{0\}$，重数为 0。
3. 若 $L' \not\subseteq U+L$：对应商空间 $V/(L+L')$ 中与 $(U+L+L')/(L+L')$ 不相交的 $(k-2)$ 维子空间数，为 $N_2 = q^{k(k-2)} \begin{bmatrix} n-k-2 \\ k-2 \end{bmatrix}_q$。

代入求和式，记 $c(U+L) = \sum_{L' \subseteq U+L} c_{L'}$：
$$N_1 c_L + N_2 \sum_{L' \not\subseteq U+L} c_{L'} = 0 \implies N_1 c_L + N_2 (1 - c(U+L)) = 0.$$
令 $\gamma = \frac{N_2}{N_1} = \frac{1}{q^k} \frac{q^{k-1}-1}{q^{n-k-1}-1}$，则 $c_L = -\gamma (1 - c(U+L))$。
此式表明：对 $(k+1)$ 维子空间 $U+L$ 中所有不在 $U$ 内的 1 维子空间 $L'$（共有 $q^k$ 个），其权重 $c_{L'}$ 均相同。对它们求和：
$$c(U+L) - c(U) = q^k [-\gamma (1 - c(U+L))] = q^k \gamma (c(U+L) - 1).$$
因为 $U \in \mathcal{F}$，故 $c(U) = \mathbf{1}_{\mathcal{F}}(U) = 1$。代入得：
$$(1 - q^k \gamma)(c(U+L) - 1) = 0.$$
由 $n > 2k$，有 $n-k-1 \ge k > k-1$，故 $q^k \gamma = \frac{q^{k-1}-1}{q^{n-k-1}-1} < 1$，即 $1 - q^k \gamma > 0$。
因此迫使 $c(U+L) - 1 = 0 \implies c(U+L) = 1$。代回 $c_L$ 的表达式：
$$c_L = -\gamma(1 - 1) = 0.$$

这严格证明了：对任意 $U \in \mathcal{F}$ 和任意 $L \not\subseteq U$，恒有 $c_L = 0$。
因此权重向量的支集满足：
$$\operatorname{supp}(c) \subseteq \bigcap_{U \in \mathcal{F}} U.$$
由于 $\sum_{L \in \mathcal{P}} c_L = 1 \ne 0$，交集必包含某个 1 维子空间 $L_0$。
从而对所有 $U \in \mathcal{F}$ 均有 $L_0 \subseteq U$，即 $\mathcal{F} \subseteq \mathcal{F}_{L_0}$。由基数相等，必有：
$$\mathcal{F} = \mathcal{F}_{L_0} = \{ U \in \mathrm{Gr}_q(n,k) : L_0 \subseteq U \}.$$
若存在另一 1 维子空间 $L_1 \ne L_0$，显然 $\mathcal{F}_{L_0} \ne \mathcal{F}_{L_1}$，故中心子空间 $L_0$ 严格唯一。

======================================================================
第四问：Lovász θ 数与 Shannon 容量 (Lovász Theta Number and Shannon Capacity)
======================================================================

一、 群作用与对称性分析

群 $\Gamma = \mathrm{GL}_n(\mathbb{F}_q)$ 自然作用于 $\mathrm{Gr}_q(n,k)$ 上，保持子空间交的维数，故 $\Gamma \le \mathrm{Aut}(G)$。
1. 顶点传递性：任取 $U_1, U_2 \in \mathrm{Gr}_q(n,k)$，将各自的一组基分别扩充为 $\mathbb{F}_q^n$ 的基，存在可逆线性变换将其相互映射，故 $G$ 顶点传递。
2. 边传递性：任取两条边 $\{U_1, W_1\}$ 与 $\{U_2, W_2\}$，因 $U_i \cap W_i = \{0\}$，基底并集在 $\mathbb{F}_q^n$ 中线性无关（维数为 $2k \le n$）。可逆变换可将两对基底同时映射，故 $G$ 边传递。

二、 Lovász $\vartheta$ 数的精确计算

Lovász $\vartheta$ 函数的对偶半定规划 (SDP) 表述为：
$$\vartheta(G) = \min \{ \lambda_{\max}(M) : M \in \mathrm{Sym}(N), M_{uu} = 1\; (\forall u), M_{uv} = 1\; (\forall \{u,v\} \notin E(G), u \ne v) \}.$$
取实数 $c = \frac{-N}{d - \lambda_{\min}} < 0$，构造实对称矩阵 $M = J + c A$。
- 当 $u = v$ 时，$M_{uu} = J_{uu} + c A_{uu} = 1 + 0 = 1$；
- 当 $u \not\sim v$ ($u \ne v$) 时，$A_{uv} = 0$，故 $M_{uv} = J_{uv} + c A_{uv} = 1 + 0 = 1$。
因此 $M$ 是可行解。

计算 $M$ 的谱：
- 全 1 向量 $\mathbf{1}$ 对应特征值：$N + c d = N - \frac{N d}{d - \lambda_{\min}} = \frac{-N \lambda_{\min}}{d - \lambda_{\min}}$；
- 与 $\mathbf{1}$ 正交的特征向量 $v \in \widetilde{E}_j$ ($j \ge 1$) 满足 $J v = 0$，对应特征值为 $c \lambda_j$。由于 $c < 0$ 且 $\lambda_j \ge \lambda_{\min}$，有 $c \lambda_j \le c \lambda_{\min} = \frac{-N \lambda_{\min}}{d - \lambda_{\min}}$。
因此 $\lambda_{\max}(M) = \frac{-N \lambda_{\min}}{d - \lambda_{\min}}$。由群平均与边传递性，最优矩阵必具有 $J + c A$ 的形式，故：
$$\vartheta(qK(n,k)) = \frac{-N \lambda_{\min}}{d - \lambda_{\min}} = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$

三、 Shannon 容量与强图积独立数

根据 Lovász 夹逼定理，对任意图 $G$：
$$\alpha(G) \le \Theta(G) \le \vartheta(G),$$
其中 Shannon 容量定义为 $\Theta(G) = \sup_{r \ge 1} \alpha(G^{\boxtimes r})^{1/r}$。
代入第二问与第四问结果：
$$\begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q = \alpha(qK(n,k)) \le \Theta(qK(n,k)) \le \vartheta(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$
两端夹逼，精确确定了 Shannon 容量：
$$\Theta(qK(n,k)) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q.$$

进一步，利用 $\vartheta$ 数关于强积的可乘性 $\vartheta(G^{\boxtimes r}) = \vartheta(G)^r$，对任意正整数 $r \ge 1$：
$$\begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r = \alpha(qK(n,k))^r \le \alpha(qK(n,k)^{\boxtimes r}) \le \vartheta(qK(n,k)^{\boxtimes r}) = \vartheta(qK(n,k))^r = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r.$$
因此对所有正整数 $r \ge 1$，恒有：
$$\alpha(qK(n,k)^{\boxtimes r}) = \begin{bmatrix} n-1 \\ k-1 \end{bmatrix}_q^r.$$
这表明 Shannon 容量由单次使用的最大相交族完全达到，多轮块编码无法提供超越平凡笛卡尔积的渐近独立数增益。全部四问严格证毕。